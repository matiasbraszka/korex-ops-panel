node_modules
dist
.env
.env.local
# Lleva la service_role_key (saltea RLS) para correr las edge fns local. Nunca al repo.
scripts/agent-fn-local.env
*.log

.vercel

# Estado local del CLI de Supabase / Deno. Es de la maquina, no del proyecto:
# .temp/linked-project.json guarda a que proyecto linkeaste, y deno.lock lo
# regenera el CLI al deployar. El repo nunca los tuvo.
supabase/.temp/
deno.lock
